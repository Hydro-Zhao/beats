{
  "description": "Pipeline for parsing gem config logs",
  "processors": [
	  {
		  "json": {
			  "field": "message",
			  "target_field": "gem5.config"
		  }
	  }
  ],
  "on_failure" : [{
    "set" : {
      "field" : "error.message",
      "value" : "{{ _ingest.on_failure_message }}"
    }
  }]
}
