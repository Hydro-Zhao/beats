{
  "description": "Pipeline for parsing gem config logs",
  "processors": [
	  {
		  "rename": {
			  "field": "json",
			  "target_field": "gem.config"
		  }
	  }
    ],
  "on_failure" : [{
    "set" : {
      "field" : "error.message",
      "value" : "{{ _ingest.on_failure_message }}"
    }
  }]
}
