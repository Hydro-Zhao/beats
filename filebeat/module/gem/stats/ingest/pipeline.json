{
  "description": "Pipeline for parsing gem stats logs",
  "processor" : [
  ],
  "on_failure" : [{
    "set" : {
      "field" : "error.message",
      "value" : "{{ _ingest.on_failure_message }}"
    }
  }]
}
